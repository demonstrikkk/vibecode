# Backend Environment Variables
# Copy this to .env for local development
# In Vercel, set these in Project Settings > Environment Variables

# ===========================================
# REQUIRED: OpenRouter API Key
# ===========================================
# Get your key from: https://openrouter.ai/keys
OPENROUTER_API_KEY=sk-or-v1-your-api-key-here

# ===========================================
# MONGODB DISABLED (Using In-Memory Storage)
# ===========================================
# MongoDB is disabled to avoid connection issues on Vercel
# All data will be stored in-memory during the session
# NOTE: Data will be lost on server restart
USE_MONGODB=false

# (Optional) MongoDB Connection String - NOT CURRENTLY USED
# For MongoDB Atlas:
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/chefbuddy_db?retryWrites=true&w=majority

# ===========================================
# REQUIRED: JWT Secret Key
# ===========================================
# Generate a random secret key for production
# Example: openssl rand -hex 32
SECRET_KEY=your-super-secret-jwt-key-change-this-in-production

# ===========================================
# OPTIONAL: OpenRouter Settings
# ===========================================
# Default model (free models available)
OPENROUTER_MODEL=x-ai/grok-4.1-fast:free
OPENROUTER_BASE=https://openrouter.ai/api/v1
